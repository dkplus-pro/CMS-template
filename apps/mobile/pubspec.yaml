name: cms_mobile
description: CMS mobile client (Flutter) — hello-world placeholder.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  dio: ^5.11.1
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.4.3
  go_router: ^18.0.1
  intl: ^0.20.3
  sentry_flutter: ^9.30.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
